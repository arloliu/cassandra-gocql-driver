# Upstream Triage TODOs — 2026-05-17

Triage of `apache/cassandra-gocql-driver` open issues and PRs against the
`otter-cache` branch. Methodology: see [`2026-05-17-plan.md`](2026-05-17-plan.md).

## Shortlist (S1/S2 + R2/R3 + A1/A2/A5)

The ranked list of items worth acting on. Within each tier, ordering is: **A5** (clean cherry-pick) before **A1** (write code now) before **A2** (investigate first). Cross-references link related issues to the PR that fixes them.

### Cherry-pick from upstream (A5) — 7 distinct PRs

These are clean, narrowly-scoped upstream PRs whose change shape does not collide with our hardening commits. Each item links to the upstream issues it resolves.

1. **[S1/A5] PR #1948** — Fix HostFilter+DC panic by counting only DCs with non-zero replicas in `topology.go:replicaMap`.
   - _Resolves:_ issue #1947 (our panic-recovery only catches the symptom; root cause remains).
   - _Next:_ cherry-pick onto otter-cache; keep our recovery wrappers as defense in depth.
2. **[S2/A5] PR #1738** — Skip down peers in `awaitSchemaAgreement` so a single down node no longer hangs the loop until timeout.
   - _Resolves:_ issues #994 and #1736 (verified to still reproduce on otter-cache — no liveness check at `conn.go:awaitSchemaAgreement`).
   - _Next:_ cherry-pick (6 lines, no overlap with our heartbeat work `1e76848`). Highest ROI of the batch.
3. **[S2/A5] PR #1790** — Honour `DisableInitialHostLookup` on reconnect/event paths (currently only consulted at session init).
   - _Resolves:_ issues #1665, #1789. (Originally listed #1721; per Codex review, #1721 is the reconnect-with-changing-IP family handled by PR #1722, not #1790.)
   - _Next:_ cherry-pick **logic only**; drop the `DisableInitialHostLookup → DisableHostLookup` rename (semver-major API break).
4. **[S2/A5] PR #1797** — Set `useSystemSchema` before policy `AddHosts` during session init so policies see the right schema version.
   - _Next:_ small reorder, safe cherry-pick.
5. **[S2/A5] PR #1724** — Apply `HostFilter` to the control connection (currently bypassed; pre-handshake `session.go:262-278` dials unfiltered hosts).
   - _Next:_ cherry-pick; review interaction with our event-handler hardening. (Inconsistency vs. issue row #1723 which classifies the same symptom S3 — keep PR at S2 because unfiltered hosts are actually dialed before rejection at `control.go:333-335`.)
6. **[S3/A5] PR #1867** — ~~Fast-fail when `HostFilter` rejects every host at session init.~~ **DONE — adopted the same day as this sweep, in `c6e7dc7`.** The guard lives at `session.go:301-303`. Re-verified 2026-09-05.
   - _Next:_ nothing. Do not re-open: the remaining `DisableInitialHostLookup` path fails fast through `control.go:342` instead, and `disableControlConn` is unexported.
7. **[S3/A5] PR #1866** — Let query context deadline override connection timeout so callers can shorten timeouts per-query. Today `conn.go:1441-1456` always arms the timeout from `c.r.GetTimeout()` and ignores `ctx.Deadline()`.
   - _Also resolves:_ issue #953 (per Codex review — was incorrectly in no-action bucket).
   - _Next:_ cherry-pick; double-check interaction with our retry caps `4808e09` / `31edd04`.

### Fix locally — write our own (A1) — 2 items

- **[S2/A1] issue #1919** — "Pool connection error" storms with small `Session.Timeout`. Heartbeat at `conn.go:732` calls `c.exec(context.Background())`, then `conn.go:1441-1456` arms the connection timeout — so a short `Session.Timeout` cascades into per-heartbeat failures. Patch direction: heartbeat should use a dedicated longer timeout independent of `Session.Timeout`.
  - _Next:_ write the patch and add a regression test with `Session.Timeout=100ms`.
- **[S2/A1] issue #1884** — Reconnecting node to cluster breaks `gocql.Session`. Needs a k8s reproducer to isolate whether `b69124b` plus our recovery work already cover it, or whether a stale-host_id rejoin is the real bug.
  - _Next:_ build a minimal k8s/ccm reproducer that recycles a node with a new IP but same host_id.

### Investigate (A2) — top picks at S2

Not blocking, but worth a code-read or short reproducer before we decide cherry-pick vs. local-fix vs. close-out.

8. **[S2/A2] issue #1582** — Rolling restart in k8s leaves all nodes marked DOWN. Partial coverage exists: `host_source.go:875-890` does remove/re-add for an existing host_id whose IP changed, but full-cluster simultaneous IP recycling (k8s pods losing all IPs at once) is not exercised. Reproducer needed for the worst case.
9. **[S2/A2] issue #812** — Query retries don't trigger because `HostPoolHostPolicy` aborts execution. **Re-verified 2026-09-05 and the shape changed; the description here is half wrong.** `f1ff52e` made `hostpool.Pick` one-shot (a `used` guard, to fix #1259's CPU spin), so the `RetryNextHost` branch at `query_executor.go:302-305` gets nil from its second `hostIter()` and exits with the retry budget unspent — returning `lastErr`, not `ErrNoConnections`, whenever a host was actually reached. Plan: `tmp/812-hostpool-retry-plan.md`.
10. **[S2/A2] issue #1259** — Narrower than originally classified. Per Codex review: the `TokenAware → HostPool` fallback path is covered by `policies.go:804-842`'s `used` map exit. Only the **standalone** `hostpool.HostPoolHostPolicy.Pick` at `hostpool/hostpool.go:121-141` retains the unbounded behavior — lower priority. Bundle with #812.
11. **[S2/A2] PR #1729** — Mark host down when control conn dial fails. Today `control.go:482-490` logs and continues without `handleNodeDown`. Small patch but PR diff not live-verified; confirm before cherry-pick.
12. **[S2/A2] PR #1722** — Random-host-ID reconciliation on first ring refresh when `DisableInitialHostLookup` is set (`session.go:308-310` assigns random IDs; `host_source.go:856-890` lacks first-refresh reconciliation). Distinct from PR #1790 — both may be needed.

### Track upstream (A3) — no action this sweep

Refactors / features without an immediate forcing function:
- **[S3/A3] PR #1944** — Cluster-metadata locking refactor (`host_source.go:487-491` and `:817-830` lock scope). Demoted from S2 per Codex: not severe enough to qualify as an S2 perf cliff without a benchmark.
- PR #1946 (segment frame batching), PR #1664 (cluster metadata exposure), PR #1796 (client connection introspection), PR #1794 (virtual keyspace metadata), PR #1645 (mis-bucketed in this sweep — actually S3/R3/A3).
- Issues #1945, #1695, #1447, #1515, #1757, #1053.
- Re-evaluate next sweep.

### Track upstream (A3) — no action this sweep

Refactors / features without an immediate forcing function: PR #1946 (segment frame batching), PR #1664 (cluster metadata exposure), PR #1796 (client connection introspection), PR #1794 (virtual keyspace metadata), issues #1945, #1695, #1447, #1515, #1757, #1053. Re-evaluate next sweep.

---

## Full triage

### Issues

---

### [S1/R2/A5] #1947 — Panic when using a HostFilter and keyspace is not replicated to every DC
- **Upstream:** https://github.com/apache/cassandra-gocql-driver/issues/1947
- **Last activity:** 2026-05-07
- **Summary:** `networkTopology.replicaMap` panics during `tokenAwareHostPolicy.updateAllReplicas` when a HostFilter restricts hosts to one DC and a keyspace has no replicas in that DC; the token map size assertion fires. Confirmed panic with a real stack trace at `topology.go:391`.
- **Our fork:** Partial: our panic-recovery (`4e89aa9` refreshDebouncer.flusher wrapper, `ec2fd58` eventDebouncer) catches the panic so the driver doesn't crash, but the underlying buggy loop in `topology.go:383-391` (`for _, dc := range n.dcs { if dc > 0 { dcsWithReplicas++ } }`) is unchanged and the replica map for that keyspace silently stays empty / repeatedly panics. Root cause remains.
- **Next step:** Cherry-pick PR #1948 — the upstream fix (`for dc, rf := range n.dcs { if _, knownDc := dcRacks[dc]; knownDc && rf > 0 ...`) is a 2-line patch with no conflict against our work; we get correct routing AND keep the recovery as defence-in-depth.

---

### [S1/R2/A0] #1559 — Token aware host policy causes panic
- **Upstream:** https://github.com/apache/cassandra-gocql-driver/issues/1559
- **Last activity:** 2025-02-27
- **Summary:** `createRoutingKey` panics with index-out-of-range when the query binding count mismatches the partition key column count; hits `policies.go:548` / `session.go:1829`. Old version but the code path still exists.
- **Our fork:** Already fixed in trunk (pre-fork): `session.go:createRoutingKey` (current line 2272) explicitly returns `errors.New("gocql: number of values does not match the number of bind columns")` when `len(values) != len(meta.BindColumns)` and uses `meta.PKBindColumnIndexes` (not raw partition-key columns) so the out-of-range index path is unreachable.
- **Next step:** None.

---

### [S1/R2/A0] #1587 — KeyspaceMetadata causes panic when describing aggregates
- **Upstream:** https://github.com/apache/cassandra-gocql-driver/issues/1587
- **Last activity:** 2025-02-27
- **Summary:** `compileMetadata` in `metadata.go:330` panics with nil dereference during `tokenAwareHostPolicy.KeyspaceChanged` / `refreshSchema`. Labeled `bug`. Triggered with a single-node cluster using TokenAwareHostPolicy.
- **Our fork:** Already fixed in trunk (pre-fork): `metadata.go:980-991` in `compileMetadata` now guards `finalFunc := keyspace.Functions[aggregates[i].finalFunc]; if finalFunc != nil { ... }` (same for `stateFunc`). The old stack-trace deref of a nil pointer is no longer reachable.
- **Next step:** None.

---

### [S1/R2/A0] #1473 — Data race when calling SetConsistency
- **Upstream:** https://github.com/apache/cassandra-gocql-driver/issues/1473
- **Last activity:** 2025-03-26
- **Summary:** `Session.SetConsistency` writes `s.cons` under a lock but `Conn.UseKeyspace` reads `c.session.cons` without synchronization; confirmed with `-race`. Occurs during pool initialization.
- **Our fork:** Not applicable because the public `Session.SetConsistency` setter no longer exists in our (and upstream trunk's) API. `Session.cons` is written once at struct init (`session.go:157 cons: cfg.Consistency`) and only read thereafter (`session.go:1121`, `2112`, `conn.go:1901`); there is no concurrent writer, so the race is gone by removal-of-API rather than by locking.
- **Next step:** None — re-open if a `SetConsistency` setter is reintroduced.

---

### [S2/R2/A1] #1884 — Reconnecting node to cluster breaks gocql.Session
- **Upstream:** https://github.com/apache/cassandra-gocql-driver/issues/1884
- **Last activity:** 2025-12-18
- **Summary:** When a Cassandra node rejoins the ring after being removed, the session stops serving queries (`gocql: no hosts available in the pool`) indefinitely. Reproducible with k8s bitnami multi-node. Likely a pool/ring reconciliation bug where the rejoining node's host-pool entry enters a permanent `filling=true` loop.
- **Our fork:** Partial: `b69124b` (respect `ReconnectionPolicy.MaxRetries`) and the hostConnPool.fill panic-guard (`4e89aa9` flips `handedOff` to keep `filling=true` from getting stuck on a panic) defend the most common ways pool.filling can latch on, and `b69124b` ensures the retry loop eventually gives up rather than spinning. But the rejoin-path bug is more likely in ring/event handling (NEW_NODE/UP_NODE arriving while the host_id is still cached with a stale ConnectAddress) which we have NOT modified; reproduces on otter-cache as of 4a1928a for that scenario.
- **Next step:** Needs k8s repro; investigate `events.go:handleNodeUp` and `ring.addOrUpdate` interaction when an old host_id reappears with a new IP.

---

### [S2/R2/A2] #1919 — Many "Pool connection error" with small Session.Timeout
- **Upstream:** https://github.com/apache/cassandra-gocql-driver/issues/1919
- **Last activity:** 2026-02-21
- **Summary:** When `Session.Timeout` is 1s or less, the heartbeat read uses the general (short) timeout instead of `ConnectTimeout`, causing i/o timeouts that close the connection and trigger rapid reconnect storms. Constant reconnects stress Cassandra even under no load.
- **Our fork:** Reproduces on otter-cache as of 4a1928a. Our `1e76848` only swallows malformed-frame heartbeat errors; the heartbeat timeout still uses `c.r.GetTimeout()` (Session.Timeout) instead of ConnectTimeout. The reconnect storm root cause remains.
- **Next step:** Patch `controlConn.heartBeat` / connection heartbeat send to use a longer/dedicated timeout independent of Session.Timeout.

---

### [S2/R2/A2] #1721 — DisableInitialHostLookup causes prolonged "no hosts available" after network instability
- **Upstream:** https://github.com/apache/cassandra-gocql-driver/issues/1721
- **Last activity:** 2025-03-05
- **Summary:** With `DisableInitialHostLookup=true`, hosts get random UUIDs on init; when `refreshRing` runs, real host_ids from `system.peers` don't match, causing pool and policy to de-duplicate by different keys (ID vs. IP). Stale pool entries with random UUIDs keep `filling=true`, starving host iteration after network flaps.
- **Our fork:** Reproduces on otter-cache as of 4a1928a. `session.go:307-310` still assigns `MustRandomUUID()` when `DisableInitialHostLookup` is set, and there is no remove-and-re-add reconciliation on first ring refresh; PR #1722 not applied.
- **Next step:** Investigate whether the ID/IP deduplication mismatch is present in our codebase; this is a structural bug affecting k8s-style deployments.

---

### [S2/R2/A2] #1582 — Rolling restart of k8s db cluster causes all nodes to remain down
- **Upstream:** https://github.com/apache/cassandra-gocql-driver/issues/1582
- **Last activity:** 2023-01-06
- **Summary:** When all cluster nodes scale to 0 and back up (with new IPs), the client never recovers; DNS topology events for `NEW_NODE` may not fire or are not acted upon. Client remains stuck with all hosts DOWN.
- **Our fork:** Reproduces on otter-cache as of 4a1928a — same class of bug as #1721/#1678; no DNS re-resolution patch applied (`dial.go:62-79` dials by pre-resolved IP), and there is no full-cluster-replacement reconciliation path.
- **Next step:** Related to #1721 and #1678. Investigate DNS hostname re-resolution and topology event handling on full cluster replacement.

---

### [S2/R2/A2] #1259 — HostPoolHostPolicy causes infinite loop/spin when used as fallback for TokenAwareHostPolicy
- **Upstream:** https://github.com/apache/cassandra-gocql-driver/issues/1259
- **Last activity:** 2025-03-27
- **Summary:** `tokenAwareHostPolicy.Pick` loops until the fallback iterator returns nil, but `hostPoolHostPolicy.Pick` never returns nil; this spins the CPU at 100%. A simple `used` guard in the returned closure fixes it.
- **Our fork:** Partial: `tokenAwareHostPolicy.Pick` in `policies.go:804-840` carries a `used map[*HostInfo]bool` and filters fallback hosts already seen, breaking the spin when used as a TokenAware fallback. But the standalone `hostpool.HostPoolHostPolicy.Pick` in `hostpool/hostpool.go:121` still has no `used` guard, so a direct user of HostPool (not via TokenAware) can still spin if the executor never gets nil. The exact symptom described in the issue (TokenAware fallback) is fixed; the underlying hostpool iterator design is not.
- **Next step:** Optional: add `used` guard inside `hostpool.HostPoolHostPolicy.Pick` to make the standalone path also bounded.

---

### [S2/R2/A5] #994 — await schema agreement never finishes if one node is down
- **Upstream:** https://github.com/apache/cassandra-gocql-driver/issues/994
- **Last activity:** 2025-03-19
- **Summary:** If a node is down but still in `system.peers`, `awaitSchemaAgreement` sees a divergent schema version forever because the down node never updates. The driver should cross-reference its own ring state to ignore known-DOWN hosts.
- **Our fork:** Reproduces on otter-cache as of 4a1928a — `conn.go:awaitSchemaAgreementWithTimeout` (line 2135) iterates `system.peers` and only filters with `isValidPeer` + empty-schema-version; no `c.session.ring.getHost(...).IsUp()` check. Identical symptom to #1736.
- **Next step:** Cherry-pick PR #1738 (6-line peer-liveness check in conn.go); no conflict with our heartbeat changes.

---

### [S2/R2/A5] #1736 — awaitSchemaAgreement not ignoring DOWN nodes
- **Upstream:** https://github.com/apache/cassandra-gocql-driver/issues/1736
- **Last activity:** 2026-02-21
- **Summary:** On a 50+ node cluster with a replaced node still lingering in `system.peers` with a stale `schema_version`, `awaitSchemaAgreement` fails because it doesn't filter out DOWN peers. The dead peer causes schema version mismatch that never resolves.
- **Our fork:** Reproduces on otter-cache as of 4a1928a — duplicate of #994 in our code (same `awaitSchemaAgreementWithTimeout` at `conn.go:2135` with no DOWN-peer filter).
- **Next step:** Cherry-pick PR #1738 — same fix as #994.

---

### [S2/R2/A2] #812 — Query retries rarely trigger because HostPoolHostPolicy aborts execution
- **Upstream:** https://github.com/apache/cassandra-gocql-driver/issues/812
- **Last activity:** 2025-10-05
- **Summary:** `queryExecutor.executeQuery` stops when `hostpoolpolicy.Pick` returns nil after all hosts time out, instead of resetting the pool for retry. With a SimpleRetryPolicy of N retries, queries actually only retry once or twice.
- **Our fork:** Reproduces on otter-cache as of 4a1928a — `query_executor.go:170-242` (`do()`) walks `hostIter` once and exits when it returns nil; the retry policy is consulted *per attempt*, but if the iterator is exhausted the retry budget is left unused. No re-pick mechanism.
- **Next step:** Add a re-Pick on iterator exhaustion when retry budget remains, gated by RetryType.

---

### [S3/R2/A0] #1938 — MapScan does not preserve top-level UDT values (returns empty map)
- **Upstream:** https://github.com/apache/cassandra-gocql-driver/issues/1938
- **Last activity:** 2026-04-29
- **Summary:** `MapScan` silently returns an empty `map[string]interface{}` for a non-null top-level `frozen<udt>` column, while `Scan` decodes correctly. No error is raised; data loss is silent.
- **Our fork:** **Already fixed in `cfb8816`** (verified post-Codex review). `iter_mapscan_test.go:282 TestMapScan_UDT_PopulatedMapDistinctAcrossRows` and `:338 TestMapScan_UDT_NullThenNonNull` explicitly cover the top-level UDT MapScan path. The `UDTTypeInfo.Unmarshal` plumbing at `marshal.go:3026-3034` handles `*interface{}` destinations correctly. Earlier Task 4 verdict was too pessimistic.
- **Next step:** None. Close-out tracking only.

---

### [S3/R2/A2] #1909 — Bug with tuple: null tuple columns return zero-value elements instead of nil
- **Upstream:** https://github.com/apache/cassandra-gocql-driver/issues/1909
- **Last activity:** 2025-11-09
- **Summary:** A null Cassandra tuple column decoded via `MapScan` returns flat zero-value elements (e.g., `c_tuple_int_text[0]=0`, `c_tuple_int_text[1]=""`) instead of nil or absence. Affects MapScan's tuple handling.
- **Our fork:** Reproduces on otter-cache as of 4a1928a. `helpers.go:258-265` `buildMapScanCache` deliberately flattens TupleTypeInfo into one map slot per element using each element's `Zero()`, which is the design that produces zero-values for nulls. Our `be44356` and `95052d8` only reject malformed length prefixes / bound element counts; they don't change the null-handling shape.
- **Next step:** Choose between (a) propagating nil for null tuple cells in the flatten path, or (b) deprecating the flat representation in MapScan. Decide before fixing.

---

### [S3/R2/A5] #1789 — Fix disableHostLookup flag logic (not respected on reconnect/events)
- **Upstream:** https://github.com/apache/cassandra-gocql-driver/issues/1789
- **Last activity:** 2025-03-18
- **Summary:** `DisableInitialHostLookup` only suppresses `system.peers` queries during initial session init; it is ignored during control-conn reconnect, schema change events, `NEW_NODE`/`REMOVED_NODE` topology events, and `AwaitSchemaAgreement`. Users relying on it (e.g., AWS Keyspaces with restricted permissions) hit unexpected peers queries.
- **Our fork:** Reproduces on otter-cache as of 4a1928a — only `session.go:282`/`session.go:417` check the flag; `host_source.go:GetHosts`, `conn.go:awaitSchemaAgreement`, and `events.go:handleNodeUp` paths all query `system.peers` unconditionally.
- **Next step:** Cherry-pick PR #1790 logic (skip the rename in our fork to avoid the semver-breaking API change).

---

### [S3/R2/A5] #1665 — `system.peers` queried even if `DisableInitialHostLookup` is true
- **Upstream:** https://github.com/apache/cassandra-gocql-driver/issues/1665
- **Last activity:** 2024-10-30
- **Summary:** Same class of bug as #1789 and #1721: on heartbeat failure and control-conn reconnect, the driver ignores `DisableInitialHostLookup` and queries `system.peers`. AWS Keyspaces users with restricted permissions are affected.
- **Our fork:** Reproduces on otter-cache as of 4a1928a — same root cause as #1789.
- **Next step:** Treat as same root cause as #1789; cherry-pick PR #1790 (logic only).

---

### [S3/R2/A2] #1723 — HostFilter not used for control connection
- **Upstream:** https://github.com/apache/cassandra-gocql-driver/issues/1723
- **Last activity:** 2025-03-14
- **Summary:** The `HostFilter` is checked for data connections but not for the control connection selection; a filtered-out (bad-state) host can be chosen as the control conn, blocking or slowing session startup.
- **Our fork:** Partial: `control.go:333` rejects a connected host post-handshake via `c.session.cfg.filterHost(host)`, so a filtered host won't *stay* as the control conn — but it is still dialed first. `session.init` (`session.go:262-278`) does not pre-filter `hosts` before passing them to `s.control.connect(hosts, true)`. Wasted dial / startup delay still reproduces.
- **Next step:** Cherry-pick PR #1724 (16-line pre-filter in session.go); no conflict.

---

### [S3/R2/A2] #1678 — Rolling restart with recycled IPs (k8s) doesn't reconnect
- **Upstream:** https://github.com/apache/cassandra-gocql-driver/issues/1678
- **Last activity:** 2025-03-04
- **Summary:** When Kubernetes pods recycle and reuse IPs, the driver fails to reconnect because `system.peers` entries for the old pod still exist or DNS doesn't re-resolve correctly. Control-conn fails with EOF and never recovers.
- **Our fork:** Reproduces on otter-cache as of 4a1928a — duplicate of #1582 in our code; the dialer uses pre-resolved IPs (`dial.go`), and there is no NEW_NODE-with-recycled-IP reconciliation in `events.go`.
- **Next step:** Related to #1582, #1721; investigate DNS hostname pinning and `NewNode` event handling.

---

### [S3/R2/A0] #1575 — Resolving DNS at connection time causes pool imbalances
- **Upstream:** https://github.com/apache/cassandra-gocql-driver/issues/1575
- **Last activity:** 2025-02-27
- **Summary:** When contact points use DNS names with multiple A-records, the pool maps connections by the originally-resolved IP, but `dialer.DialContext` re-resolves the hostname and may get a different IP; resulting in imbalanced connections (some hosts get 200, others get 1–2 connections).
- **Our fork:** Not applicable because `defaultHostDialer.DialHost` (`dial.go:62-79`) dials `host.ConnectAddressAndPort()` which is the pre-resolved IP+port; DNS re-resolution at dial time is already prevented.
- **Next step:** None.

---

### [S3/R2/A2] #1707 — Speculative execution returns error when all replicas taken by main execution
- **Upstream:** https://github.com/apache/cassandra-gocql-driver/issues/1707
- **Last activity:** 2024-07-15
- **Summary:** When speculative execution fires but can't find an available replica (all taken by main execution), it publishes `ErrNoConnections` to the result channel; this error is returned to the user even though the main execution may still succeed. Reproducible with `TimeoutDelay: 1`.
- **Our fork:** Reproduces on otter-cache as of 4a1928a. `query_executor.go:executeQuery` (line 158) selects on a `results` channel of size 1; the first goroutine to send wins. A speculative goroutine exhausting `hostIter` and returning `ErrNoConnections` can win the race against a slower-but-successful main attempt.
- **Next step:** Prefer non-error results in the executor (drain results, keep the success) or treat `ErrNoConnections` from a spec attempt as "abandon this attempt, wait for others".

---

### [S3/R2/A2] #1084 — Invalid data returned after adding column via ALTER TABLE
- **Upstream:** https://github.com/apache/cassandra-gocql-driver/issues/1084
- **Last activity:** 2025-03-27
- **Summary:** After `ALTER TABLE ... ADD COLUMN`, `MapScan` returns garbled or missing data for some queries (likely stale prepared statement or cached column metadata). Reproducible on a 27-node cluster; not on 3-node CCM.
- **Our fork:** Reproduces on otter-cache as of 4a1928a. Our `4808e09`/`31edd04` cap re-prepare on `RequestErrUnprepared` and trigger eviction on that server-side signal, but there is no client-side prepared-cache invalidation on a `SCHEMA_CHANGE` event for a table; if Cassandra keeps serving the same prepared statement with stale result metadata after ALTER, the driver won't re-prepare.
- **Next step:** On `SchemaChangeTypeUpdated` for a table, invalidate all prepared statements referencing it from `stmtsLRU`.

---

### [S3/R2/A3] #1945 — Reduce mutex contention around host_source and ring refresh
- **Upstream:** https://github.com/apache/cassandra-gocql-driver/issues/1945
- **Last activity:** 2026-05-15
- **Summary:** Production mutex profiling shows significant contention in `host_source` ring refresh paths; PR #1944 exists upstream with a fix. Issue is a performance concern, not a correctness bug.
- **Our fork:** Reproduces on otter-cache as of 4a1928a — `ringDescriber` still has `mu`, `prevHosts`, `prevPartitioner` fields; `GetHosts` holds the lock across blocking I/O. Our `956fee3` optimized the *routing-info* cache, not host_source itself.
- **Next step:** Track upstream PR #1944; cherry-pick once merged. No conflict expected with `956fee3` because the touched fields are disjoint.

---

### [S3/R2/A2] #1699 — MapScan returns zero instead of nil for null values
- **Upstream:** https://github.com/apache/cassandra-gocql-driver/issues/1699
- **Last activity:** 2025-01-03
- **Summary:** `MapScan` returns zero values (empty string, false, 0) for null Cassandra columns instead of nil, making it impossible to distinguish null from zero. PR #1834 exists upstream tracking a fix.
- **Our fork:** Reproduces on otter-cache as of 4a1928a — `helpers.go:200-244` MapScan uses `TypeInfo.Zero()` for each column and stores the dereferenced result; null columns get the type's zero value, not nil.
- **Next step:** Either adopt PR #1834's additive `MapScanWithNullableValues` or change the existing MapScan semantics (breaking).

---

### [S3/R2/A2] #727 — SliceMap copies previous row's value when column is nil
- **Upstream:** https://github.com/apache/cassandra-gocql-driver/issues/727
- **Last activity:** 2021-12-22
- **Summary:** When rows contain null timestamp (and possibly other) columns, `SliceMap` returns the previous row's value for those columns instead of nil/zero. `MapScan` does not exhibit this. Labeled `waiting-for-info`; narrow path but silent data corruption.
- **Our fork:** Partial: `helpers.go:215-226` MapScan was hardened to *refresh* the per-slot zero on each row (with the explicit comment about preventing aliasing). That removes the cross-row-aliasing bite for MapScan, and `SliceMap` (helpers.go:137-156) calls into a fresh `map[string]interface{}` per row → the previous-row leak should be gone. But null still becomes the type's zero (not nil), which is the related #1699/#1248 ergonomic issue.
- **Next step:** Treat as covered by `956fee3` per-row zero refresh for the "previous row's value" bite; fix null-vs-zero via #1699 path.

---

### [S3/R2/A2] #1248 — NULL text/boolean/blob returned as zero value by SliceMap
- **Upstream:** https://github.com/apache/cassandra-gocql-driver/issues/1248
- **Last activity:** 2025-02-20
- **Summary:** `SliceMap` and `MapScan` return zero values for null scalar columns (e.g., empty string for null text, false for null boolean) instead of nil. Related to #1699; long-standing ergonomics issue affecting null-aware applications.
- **Our fork:** Reproduces on otter-cache as of 4a1928a — duplicate of #1699 root cause.
- **Next step:** Same root cause as #1699; fix once.

---

### [S3/R2/A2] #1643 — Null data unmarshal to pointer-of-pointer is not nil
- **Upstream:** https://github.com/apache/cassandra-gocql-driver/issues/1643
- **Last activity:** 2022-08-31
- **Summary:** When unmarshaling a null CQL value into a `**T` destination, the result is a non-nil pointer-to-zero instead of nil. A PR exists (#1641) but tests don't pass. Narrow edge case.
- **Our fork:** Reproduces on otter-cache as of 4a1928a — no specific `**T`-null handling in the reflect path of Unmarshal; our `cfb8816`/`95052d8` hardening doesn't touch this shape.
- **Next step:** Low priority; verify whether our marshal path correctly nil-propagates through pointer-of-pointer.

---

### [S3/R3/A3] #1695 — Memory allocations optimization (TypeInfo receiver type)
- **Upstream:** https://github.com/apache/cassandra-gocql-driver/issues/1695
- **Last activity:** 2025-06-06
- **Summary:** `CollectionType`, `UDTTypeInfo` etc. use value receivers for `NewWithError`/`New` causing per-call struct copies; switching to pointer receivers reduces allocations ~38% in benchmarks. `semver-major` label; our fork has done related perf work.
- **Our fork:** Reproduces on otter-cache as of 4a1928a — `CollectionType` and `UDTTypeInfo` methods at `marshal.go:1828-2155, 2917-3113` are still value receivers. Our `956fee3` perf work did NOT switch them.
- **Next step:** Track upstream (`semver-major`); consider applying after a major release.

---

### [S3/R3/A3] #1447 — CASSGO-22: Don't mutate Query/Batch
- **Upstream:** https://github.com/apache/cassandra-gocql-driver/issues/1447
- **Last activity:** 2025-02-25
- **Summary:** Queries are mutated during execution (metrics, consistency changes for speculation); this is unsafe for shared/reused queries. Labeled `semver-major`. Broad refactor to move metrics to Iter.
- **Our fork:** Reproduces on otter-cache as of 4a1928a — same architecture as upstream; we haven't moved metrics off Query/Batch.
- **Next step:** Track upstream; large API change, low urgency for our fork.

---

### [S3/R3/A3] #1515 — Use errors.Is/errors.As to check errors
- **Upstream:** https://github.com/apache/cassandra-gocql-driver/issues/1515
- **Last activity:** 2025-02-26
- **Summary:** Error checking in several places (e.g., `connectionpool.go`) uses exact type assertions rather than `errors.Is`/`errors.As`; our fork already uses `%w` for wrapping (`6238950`), but sentinel comparisons may still be brittle.
- **Our fork:** Partial: `6238950` wraps 22 fmt.Errorf sites with `%w` so callers *can* now use `errors.Is/As`, and we use these helpers in 24 places. But the call-site audit (e.g. `conn.go:697 err, ok := p.frame.(error)`, `dial.go logConnectErr` type-switching on `*net.OpError`) is not exhaustive — still uses raw type assertions in places.
- **Next step:** Targeted audit of error-check sites in our fork that *we* wrote or modified; do not touch unmodified upstream sites.

---

### [S3/R3/A3] #1757 — cowHostList uses outdated logic for host uniqueness
- **Upstream:** https://github.com/apache/cassandra-gocql-driver/issues/1757
- **Last activity:** 2024-05-30
- **Summary:** `cowHostList.add` deduplicates by `ConnectAddress` equality, but when hosts are behind a proxy (address translator mapping all nodes to one address with different ports), only one host survives dedup and node failures cause `ErrNoConnections`. Niche but real production issue.
- **Our fork:** Reproduces on otter-cache as of 4a1928a — `cowHostList.add` (`policies.go:61-83`) uses `host.Equal(l[i])` which delegates to `ConnectAddress().Equal(...)` (`host_source.go:211`); same dedup flaw.
- **Next step:** Track upstream PR #1758.

---

### [S3/R3/A3] #1053 — metadata: handle UDT types correctly
- **Upstream:** https://github.com/apache/cassandra-gocql-driver/issues/1053
- **Last activity:** 2025-02-27
- **Summary:** UDTs in metadata are parsed as `NativeType{typ: TypeCustom}` instead of being looked up from `system_schema.types` and built as a proper `UDTType`. Enhancement label; affects tooling using `KeyspaceMetadata`.
- **Our fork:** Reproduces on otter-cache as of 4a1928a — we have not modified the metadata UDT parsing path.
- **Next step:** Low priority; only matters if we use `KeyspaceMetadata` for UDT introspection.

---

### PRs

---

### [S1/R2/A5] PR #1948 — CASSGO-122 Fix panic when using a HostFilter and keyspace is not replicated to every DC
- **Upstream:** https://github.com/apache/cassandra-gocql-driver/pull/1948
- **Last activity:** 2026-05-15 (created 2026-05-07)
- **Files touched:** `topology.go`, `topology_test.go`
- **Summary:** Adds a nil/empty guard in `topology.go` when building the replica map for a keyspace that has no replicas in the filtered DC; previously caused a panic in `tokenAwareHostPolicy.updateAllReplicas`. Small, clean patch (+35/-2).
- **Our fork:** Confirmed not applied. `topology.go:383-391` on otter-cache still has the unguarded `for _, dc := range n.dcs { if dc > 0 ...` loop and the size-mismatch `panic(...)`; our panic-recovery hides the symptom but the keyspace is unroutable until upstream's `knownDc` filter is applied.
- **Next step:** Cherry-pick PR #1948. The diff only touches `topology.go:383-388` — no overlap with our `956fee3` lock-free routing-info work (that one cached `replicas` at the policy layer, not at topology).

---

### [S2/R2/A5] PR #1797 — fix: set useSystemSchema before policy setup
- **Upstream:** https://github.com/apache/cassandra-gocql-driver/pull/1797
- **Last activity:** 2024-08-14 (created 2024-08-13)
- **Files touched:** `session.go`
- **Summary:** During `NewSession`, `AddHosts` is called before `useSystemSchema` is set, so `getKeyspaceMetadata` uses the wrong system table (`system.schema_keyspaces` vs `system_schema.keyspaces`). Reorders initialization in `session.go` (+13/-13).
- **Our fork:** Confirmed not applied — `session.go` still calls `AddHosts` (line 389) before setting `useSystemSchema` (line 417-423). Same ordering as origin/trunk.
- **Next step:** Small session.go reorder; cherry-pick PR #1797.

---

### [S2/R2/A5] PR #1738 — Ensure peer is up when waiting schema agreement
- **Upstream:** https://github.com/apache/cassandra-gocql-driver/pull/1738
- **Last activity:** 2024-07-30 (created 2024-02-08)
- **Files touched:** `conn.go`
- **Summary:** Adds a liveness check against the driver's ring state before including a peer's schema version in `awaitSchemaAgreement`, preventing indefinite hangs when a replaced/dead node lingers in `system.peers`. Fixes issues #994 and #1736. Very small (+6/-0).
- **Our fork:** Confirmed not applied — `conn.go:awaitSchemaAgreementWithTimeout` (line 2135) has no `c.session.ring.getHost(...).IsUp()` check.
- **Next step:** Cherry-pick. The PR inserts 6 lines around `host.schemaVersion` filtering; no conflict with our `1e76848` (which touches heartbeat-frame parsing, not awaitSchemaAgreement).

---

### [S2/R2/A0] PR #1952 — Cap RequestErrUnprepared retry recursion on query and batch paths
- **Upstream:** https://github.com/apache/cassandra-gocql-driver/pull/1952
- **Last activity:** 2026-05-17 (created 2026-05-17)
- **Files touched:** `CHANGELOG.md`, `conn.go`, `conn_test.go`, `unprep_retry_test.go`
- **Summary:** Upstream implementation of what our commits `4808e09` and `31edd04` already do — caps recursive retry on `RequestErrUnprepared` for both query and batch paths. The upstream PR may differ in implementation details or scope.
- **Our fork:** Already fixed in 31edd04 ("fix: cap RequestErrUnprepared retry recursion (audit §4)") and 4808e09 ("fix: cap RequestErrUnprepared retry recursion on batch path"). Identical shape to PR #1952 — same `maxUnprepRetries = 5`, same `executeQueryWithUnprepRetries`/`executeBatchWithUnprepRetries` wrappers, same `%w` wrapping. We also have unit tests (`TestExecuteBatch_UnprepRetryIsCapped` and the query counterpart).
- **Next step:** None — equivalent and arguably better-tested on our side.

---

### [S2/R2/A2] PR #1944 — CASSGO-121 Improve host_source locking and ring refresh concurrency
- **Upstream:** https://github.com/apache/cassandra-gocql-driver/pull/1944
- **Last activity:** 2026-05-15 (created 2026-04-16)
- **Files touched:** `host_source.go`
- **Summary:** Removes unnecessary mutex scope around blocking control-connection I/O in `getHosts`; drops stale never-written `prevHosts`/`prevPartitioner` fields. Small refactor (+18/-16) with measurable lock-contention reduction.
- **Our fork:** Confirmed not applied — `ringDescriber` (host_source.go:487) still has `mu`, `prevHosts`, `prevPartitioner`; `GetHosts` (line 818-820) still holds `mu` across blocking I/O. Our `956fee3`/`9643b4f` touch policies.go and the routing-info cache, not host_source's ringDescriber lock.
- **Next step:** Track upstream PR #1944; no conflict expected with our changes.

---

### [S2/R2/A5] PR #1790 — CASSGO-5 Fix DisableInitialHostLookup flag ignored when querying system.peers
- **Upstream:** https://github.com/apache/cassandra-gocql-driver/pull/1790
- **Last activity:** 2025-04-24 (created 2024-08-01)
- **Files touched:** `CHANGELOG.md`, `cassandra_test.go`, `host_source.go`, `session.go`
- **Summary:** Fixes `DisableInitialHostLookup` being ignored on control-conn reconnect; also renames flag to `DisableHostLookup` and propagates the check to peers-query callsites in `host_source.go`. Closes issues #1665, #1789.
- **Our fork:** Confirmed not applied — only `session.go:282` and `:417` check the flag; peers-query callsites in `host_source.go` and `conn.go:awaitSchemaAgreement` ignore it.
- **Next step:** Cherry-pick the *logic* fix only (skip the `DisableInitialHostLookup` → `DisableHostLookup` rename to avoid a semver-breaking API change in our fork).

---

### [S2/R2/A0] PR #1785 — CASSGO-6 Fix 'Unable to discover cluster nodes with an empty rack name'
- **Upstream:** https://github.com/apache/cassandra-gocql-driver/pull/1785
- **Last activity:** 2025-06-19 (created 2024-07-24)
- **Files touched:** `CHANGELOG.md`, `cluster_test.go`, `helpers.go`, `host_source.go`
- **Summary:** Fixes `isValidPeer()` to treat empty rack string as valid (only null/absent rack is invalid); previously rejected nodes with empty rack names, preventing cluster discovery. Addresses issue #1706.
- **Our fork:** Already covered in origin/trunk (pre-fork) — `host_source.go:809-815 isValidPeer` checks `host.missingRack` (a boolean flag set only when rack is null/absent, not empty), and `HostInfo.missingRack` plus its set sites are present on both trunk and otter-cache. Either PR #1785 has been merged or an equivalent fix predates it.
- **Next step:** None.

---

### [S2/R2/A2] PR #1729 — Mark node as down if unable to dial control conn
- **Upstream:** https://github.com/apache/cassandra-gocql-driver/pull/1729
- **Last activity:** 2024-07-30 (created 2023-10-13)
- **Files touched:** `control.go`
- **Summary:** Restores a previously-deleted `nodeDown` call in `control.go` so that when the control connection to a node fails to dial, the driver marks that host down (re-triggers reconnect logic). Without this, the node stays in limbo. Small (+3/-0).
- **Our fork:** Confirmed not applied — `control.go:482-503` loops `c.session.connect`, logs failures via `c.session.logger.Info`, but never calls `handleNodeDown` on dial failure.
- **Next step:** Cherry-pick candidate; check whether the 3-line nodeDown insertion lands cleanly in our control.go shape.

---

### [S2/R2/A5] PR #1724 — HostFilter Not Used for Control Connection
- **Upstream:** https://github.com/apache/cassandra-gocql-driver/pull/1724
- **Last activity:** 2025-03-25 (created 2023-09-14)
- **Files touched:** `session.go`
- **Summary:** Applies `HostFilter` when selecting the initial control connection host; previously a filtered-out (bad-state) host could be chosen as control conn, blocking session startup. Also handles `disableControlConn`/`DisableInitialHostLookup` edge cases. (+16/-0)
- **Our fork:** Confirmed not applied — `session.go:262-278` passes `hosts` to `s.control.connect(hosts, true)` without pre-filtering. We do reject in `control.go:333` post-handshake (so a filtered host won't *stay*), but the wasted dial / startup delay remains.
- **Next step:** Cherry-pick PR #1724 (16-line pre-filter in session.init); no conflict.

---

### [S2/R2/A2] PR #1722 — If DisableInitialHostLookup, Update Hosts On First Ring Refresh
- **Upstream:** https://github.com/apache/cassandra-gocql-driver/pull/1722
- **Last activity:** 2025-03-05 (created 2023-09-08)
- **Files touched:** `AUTHORS`, `common_test.go`, `host_source.go`, `integration_test.go`
- **Summary:** When `DisableInitialHostLookup` is set, hosts start with random UUIDs; on first ring refresh the real host_ids from `system.peers` differ, causing stale entries. This PR removes and re-adds hosts on first refresh to reconcile IDs. Closes #1721.
- **Our fork:** Confirmed not applied — `session.go:308-310` assigns `MustRandomUUID()` to hosts missing HostID, and `host_source.go` ring-refresh path has no first-refresh-reconciliation logic.
- **Next step:** Investigate compatibility with our `host_source.go` / ring-refresh changes (our diff there is small — `6238950` %w wrapping + `4e89aa9` recovery only) before applying.

---

### [S2/R2/A0] PR #1352 — Fix context leaking between goroutines
- **Upstream:** https://github.com/apache/cassandra-gocql-driver/pull/1352
- **Last activity:** 2024-07-30 (created 2019-09-23)
- **Files touched:** `cassandra_test.go`, `conn.go`, `prepared_cache.go`, `session.go`
- **Summary:** When multiple goroutines share an inflight prepare request and the first goroutine's context is canceled, all waiting goroutines received `context.Canceled`; this PR fixes inflight request sharing in `prepareStatement` so each waiter gets its own result. Large (+188/-74).
- **Our fork:** Handled differently on our fork: `7045947` ("Replace preparedLRU mutex with otter for lock-free concurrency") + `f284695` plus `conn.go:1591-1593` `c.prepareStatement` now uses `c.ctx` (connection context) — *not* the caller's ctx — for the loader call inside `otter.LoaderFunc`. So a caller canceling its ctx no longer propagates to waiters who share the loader. The reported symptom is gone.
- **Next step:** None.

---

### [S2/R2/A0] PR #1486 — conn: prevent potential race during writeCoalese stop method
- **Upstream:** https://github.com/apache/cassandra-gocql-driver/pull/1486
- **Last activity:** 2024-07-30 (created 2020-08-28)
- **Files touched:** `conn.go`, `conn_test.go`
- **Summary:** Prevents a potential data race in `writeCoalesce.stop()` where the `stopped` channel can be sent to after it's already closed under concurrent stop calls. Adds synchronization to the stop path (+22/-12).
- **Our fork:** Not applicable because our `writeCoalescer` (conn.go:1167+) has no explicit `stop()` channel-close path; the flusher exits on context cancel (`writeFlusherImpl` selects on `ctx.Done()`) and is wrapped by `handleRecoveredPanic`. The exact race surface from the upstream PR doesn't exist here.
- **Next step:** None.

---

### [S3/R2/A5] PR #1867 — CASSGO-62 Check that at least one host passed on initial host lookup
- **Upstream:** https://github.com/apache/cassandra-gocql-driver/pull/1867
- **Last activity:** 2025-04-01 (created 2025-03-06)
- **Files touched:** `session.go`
- **Summary:** Adds a guard during session init to return an error if `HostFilter` rejects all discovered hosts, preventing a silent no-hosts state. Resolves issue #738. Very small (+3/-0).
- **Our fork:** ~~Confirmed not applied.~~ **Adopted in `c6e7dc7` on 2026-05-17, hours after this sweep was written.** The guard is at `session.go:301-303`, in the initial-host-lookup branch. The original "not applied" note looked only at the pool-fill loop (now `session.go:339`) and missed it, and the 2026-09-04 re-verification repeated the same mistake.
- **Next step:** None. When `DisableInitialHostLookup` is set the filter is not consulted before the pool-fill loop, but `control.go:342` rejects a filtered control host and `control.connect` surfaces it as `unable to connect to initial hosts: host was filtered: ...` (`control.go:300-302`), so that path also fails fast with a clear cause. `disableControlConn` is unexported and test-only.

---

### [S3/R2/A5] PR #1866 — CASSGO-61 Fix connection timeout override with custom context timeout
- **Upstream:** https://github.com/apache/cassandra-gocql-driver/pull/1866
- **Last activity:** 2025-04-01 (created 2025-02-27)
- **Files touched:** `cassandra_test.go`, `conn.go`, `session.go`
- **Summary:** Allows query context timeout to override the default connection timeout, so callers using `context.WithTimeout` get expected behavior on slow connections. Resolves issue #953.
- **Our fork:** Confirmed not applied — `conn.go:execInternal` does not consult `ctx.Deadline()` to suppress the call timer; the upstream PR adds `_, isDeadline := ctx.Deadline(); if timeout > 0 && !isDeadline { ... }` and we have no equivalent.
- **Next step:** Cherry-pick the conn.go changes. `1e76848` touches frame-parse error handling in `recv`, not `execInternal`'s deadline path; no conflict.

---

### [S3/R3/A3] PR #1946 — CASSGO-100 Support frames batching into segment
- **Upstream:** https://github.com/apache/cassandra-gocql-driver/pull/1946
- **Last activity:** 2026-05-04 (created 2026-04-24)
- **Files touched:** `cassandra_test.go`, `conn.go`, `conn_test.go`, `control.go`, `frame.go`, `frame_test.go`, `segment_codec.go`, `segment_codec_test.go`
- **Summary:** Adds CQL protocol segment/batch framing support (large feature, +1275/-666). Touches frame.go and conn.go deeply — paths we have heavily modified for frame hardening and heartbeat fixes.
- **Our fork:** Reproduces on otter-cache as of 4a1928a in the sense that segment framing is not supported; large feature, not yet present. Will conflict heavily with our frame-parser hardening (`04993ec`, `95052d8`, `be44356`) and conn.go (`1e76848`) once we attempt to adopt.
- **Next step:** Track upstream merge; this is a major feature, not a bug fix. Re-evaluate after upstream lands.

---

### [S3/R2/A2] PR #1942 — CASSGO-118 Encode nil map value for UDTs as NULL
- **Upstream:** https://github.com/apache/cassandra-gocql-driver/pull/1942
- **Last activity:** 2026-04-24 (created 2026-04-07)
- **Files touched:** `cluster.go`, `cluster_test.go`, `integration_test.go`, `marshal.go`, `marshal_test.go`, `session.go`, `types.go`
- **Summary:** Changes UDT marshal behavior: nil Go map now encodes as a CQL NULL instead of a zero-value UDT struct. Correctness fix for UDT-typed columns. Touches marshal.go which overlaps our marshal hardening.
- **Our fork:** Reproduces on otter-cache as of 4a1928a — `UDTTypeInfo.Marshal` (marshal.go:2922) does not special-case a nil `map[string]interface{}` as NULL. Our `cfb8816`/`95052d8` only added panic guards and length-prefix rejection, not NULL semantics.
- **Next step:** Investigate marshal.go compatibility with our UDT hardening before cherry-picking; the touched lines overlap.

---

### [S3/R2/A2] PR #1834 — CASSGO-36 MapScan nil instead of zero value for null values
- **Upstream:** https://github.com/apache/cassandra-gocql-driver/pull/1834
- **Last activity:** 2025-06-09 (created 2024-10-15)
- **Files touched:** `cassandra_test.go`, `common_test.go`, `helpers.go`, `marshal.go`, `session.go`
- **Summary:** Adds a new `MapScanWithNullableValues` method that returns `nil` for null columns instead of zero values, closing issue #1699. Doesn't change existing `MapScan` behavior — additive API.
- **Our fork:** Reproduces on otter-cache as of 4a1928a — we have no `MapScanWithNullableValues` API and our MapScan (`helpers.go:200`) returns zero values for nulls.
- **Next step:** Additive API change; investigate adopting once upstream merges.

---

### [S3/R2/A2] PR #1758 — Fix cowHostList can't have hosts with same ConnectAddress
- **Upstream:** https://github.com/apache/cassandra-gocql-driver/pull/1758
- **Last activity:** 2025-05-02 (created 2024-05-31)
- **Files touched:** `host_source.go`, `policies.go`, `policies_test.go`
- **Summary:** Changes `cowHostList` deduplication to use host UUID instead of `ConnectAddress`, fixing proxy/TCP-proxy deployments where multiple Cassandra nodes share the same visible address. Closes issue #1757.
- **Our fork:** Reproduces on otter-cache as of 4a1928a — `cowHostList.add` (`policies.go:70`) uses `host.Equal(l[i])` which compares ConnectAddress.
- **Next step:** Check upstream merge status; cherry-pick after careful review against our `9643b4f` tier-rotation work (the two touch policies.go in different places — add/remove vs Pick rotation).

---

### [S3/R2/A0] PR #1576 — Fix pool imbalance when using DNS for contact points
- **Upstream:** https://github.com/apache/cassandra-gocql-driver/pull/1576
- **Last activity:** 2024-07-30 (created 2021-08-17)
- **Files touched:** `conn.go`, `host_source.go`
- **Summary:** Pins the dialer to use the pre-resolved `connectAddress` IP rather than re-resolving the hostname on each pool connection dial; prevents A-record round-robin from creating address/HostInfo mismatches and pool imbalances. Closes issue #1575.
- **Our fork:** Not applicable: `dial.go:62-79 defaultHostDialer.DialHost` already dials `host.ConnectAddressAndPort()` (the pre-resolved IP+port). The fix's symptom is gone in our (and upstream trunk's) current shape.
- **Next step:** None.

---

### [S3/R2/A2] PR #1543 — Fix call timer handling in conn.go
- **Upstream:** https://github.com/apache/cassandra-gocql-driver/pull/1543
- **Last activity:** 2024-07-30 (created 2021-03-10)
- **Files touched:** `conn.go`
- **Summary:** Fixes call timer: the `else` branch in timer init was unreachable; also adds mutex protection for `timer` field accessed from multiple goroutines (writer and recv). Fixes a potential data race in the request timeout path (+32/-18).
- **Our fork:** Reproduces on otter-cache as of 4a1928a — `callReq.timer` (conn.go:1078) has no mutex; `execInternal` writes to it (line 1443-1456) while `recv` path reads it (line 892-893).
- **Next step:** Old PR; verify whether the upstream patch still applies cleanly to our conn.go shape before cherry-picking.

---

### [S3/R2/A0] PR #1492 — Connection Dial should use connect address not hostname
- **Upstream:** https://github.com/apache/cassandra-gocql-driver/pull/1492
- **Last activity:** 2024-07-30 (created 2020-09-27)
- **Files touched:** `conn.go`, `host_source.go`
- **Summary:** Ensures the dialer uses the resolved `connectAddress` IP, not the original hostname, preventing re-resolution producing different IPs than expected. Overlaps with #1576 and #1670 in intent.
- **Our fork:** Not applicable — same reasoning as #1576/#1670. `dial.go:62-79` dials by `ConnectAddressAndPort` (pre-resolved IP).
- **Next step:** None.

---

### [S3/R2/A2] PR #1372 — executor: mark host with error if host is not up
- **Upstream:** https://github.com/apache/cassandra-gocql-driver/pull/1372
- **Last activity:** 2024-07-30 (created 2019-11-08)
- **Files touched:** `query_executor.go`
- **Summary:** In `queryExecutor.executeQuery`, if the chosen host is not in the `isUp` state, marks it with an error via `SelectedHost.Mark`; this unblocks policies like `HostPool` that need explicit error signals to rotate away from a dead host.
- **Our fork:** Reproduces on otter-cache as of 4a1928a — `query_executor.go:178-181 do()` does `if host == nil || !host.IsUp() { selectedHost = hostIter(); continue }` without calling `selectedHost.Mark(...)`. HostPool-style policies don't get the signal.
- **Next step:** Cherry-pick if upstream still has it; or apply 1-line `selectedHost.Mark(somethingErr)` ourselves.

---

### [S3/R2/A2] PR #1415 — Fix uint64 overflow and randomise initial offset
- **Upstream:** https://github.com/apache/cassandra-gocql-driver/pull/1415
- **Last activity:** 2024-07-30 (created 2020-03-06)
- **Files touched:** `policies.go`
- **Summary:** Fixes potential uint64 overflow in host-list offset calculation and randomizes the initial offset to avoid thundering-herd when multiple clients start simultaneously. Small change in `policies.go` (+18/-14).
- **Our fork:** Partial: `roundRobbin` (`policies.go:895-926`) uses `(shift + currentlyObserved) % currentLayerSize` where shift is `int(atomic.AddUint64(...))`. On 64-bit, overflow risk is far-future; on 32-bit, conversion can produce negative shifts. Randomized initial offset (thundering-herd mitigation) is not present.
- **Next step:** Check our offset/rotation code in policies.go; low priority on 64-bit.

---

### [S3/R2/A0] PR #1670 — Don't re-resolve DNS address in dialer
- **Upstream:** https://github.com/apache/cassandra-gocql-driver/pull/1670
- **Last activity:** 2024-07-30 (created 2023-01-11)
- **Files touched:** `conn.go`
- **Summary:** Prevents `HostDialer` from re-resolving the hostname when a `connectAddress` IP is already known; avoids the mismatch that causes pool imbalances. Complements #1492 and #1576 with a minimal conn.go change (+6/-2).
- **Our fork:** Not applicable — same reasoning as #1576/#1492; `dial.go:62-79` already dials by pre-resolved IP.
- **Next step:** None.

---

### [S3/R5/A0] PR #1908 — feat: replace custom LRU cache with otter cache library
- **Upstream:** https://github.com/apache/cassandra-gocql-driver/pull/1908
- **Last activity:** 2025-11-20 (created 2025-09-01)
- **Files touched:** `conn.go`, `go.mod`, `go.sum`, `prepared_cache.go`, `session.go`
- **Summary:** Upstream proposal to swap the custom LRU prepared-statement cache for the `maypok86/otter` library — exactly what our `otter-cache` branch is named for and built on. Our fork already implements this.
- **Our fork:** Covered by our entire otter-cache branch foundation.
- **Next step:** No action; we lead upstream on this.

---

### [S3/R3/A3] PR #1846 — fix nul der (fix nil dereference)
- **Upstream:** https://github.com/apache/cassandra-gocql-driver/pull/1846
- **Last activity:** 2024-12-06 (created 2024-12-05)
- **Files touched:** `conn.go`, `control.go`, `marshal.go`
- **Summary:** Cryptically titled; body is empty. Touches conn.go, control.go, and marshal.go — all paths we've heavily modified. Without a description, this is unclassifiable from title alone and warrants closer inspection.
- **Our fork:** Three concerns: (a) the control.go `oldConn != nil && oldConn.conn != conn` nil-guard is ALREADY in trunk and our `control.go:516`; (b) the conn.go `TypeHintedValue` plumbing and (c) the marshal.go `TypeHintedValue` type are an unrelated *feature* (non-prepared statement type hints), not a "nil dereference" fix at all. Misclassified upstream PR.
- **Next step:** Track upstream; if maintainers merge the feature, evaluate then. No action now.

---

### [S4/R3/A3] PR #1554 — Clarify errors in marshalList
- **Upstream:** https://github.com/apache/cassandra-gocql-driver/pull/1554
- **Last activity:** 2024-07-30 (created 2021-04-24)
- **Files touched:** `marshal.go`
- **Summary:** Improves error messages returned by `marshalList` to be more informative (+2/-2). Related to #1551.
- **Our fork:** Unknown — verify in Task 4.
- **Next step:** Trivial; apply if our marshal.go doesn't conflict.

---

### [S3/R2/A3] PR #1664 — Expose cluster metadata and token ranges
- **Upstream:** https://github.com/apache/cassandra-gocql-driver/pull/1664
- **Last activity:** 2025-03-10 (created 2022-11-14)
- **Files touched:** `cluster.go`, `cluster_metadata.go`, `events.go`, `host_source.go`, `policies.go`, `ring.go`, `session.go`, `token.go`, `topology.go`, and more (833 additions)
- **Summary:** Large refactor extracting `TokenAwareHostPolicy` cluster metadata into a standalone `ClusterMetadata` manager and exposes `Session.ClusterMetadata()` / `TokenRing.GetHostsForToken()`. Touches topology.go, policies.go, ring.go — all paths we've modified.
- **Our fork:** Reproduces on otter-cache as of 4a1928a (i.e. we don't expose `Session.ClusterMetadata()`). Will conflict with our lock-free routing-info (`956fee3`) and topology changes when applied.
- **Next step:** Track upstream; too large and risky to cherry-pick until upstream merges.

---

### [S3/R2/A3] PR #1796 — issue-1774: Method to retrieve info about client connections
- **Upstream:** https://github.com/apache/cassandra-gocql-driver/pull/1796
- **Last activity:** 2025-03-17 (created 2024-08-12)
- **Files touched:** `session.go`, `session_test.go`
- **Summary:** Adds `Session.GetClientConnections()` returning a `[]ClientConnection` with per-connection metadata (address, driver version, stage). Feature, not a bug fix. (+174/-10)
- **Our fork:** Reproduces on otter-cache as of 4a1928a — no `Session.GetClientConnections` API (`git grep` returns no hits).
- **Next step:** Track upstream; useful observability feature but not urgent.

---

### [S3/R2/A3] PR #1794 — Virtual keyspace metadata support
- **Upstream:** https://github.com/apache/cassandra-gocql-driver/pull/1794
- **Last activity:** 2025-03-21 (created 2024-08-08)
- **Files touched:** (not fetched — title indicates virtual keyspace feature)
- **Summary:** Adds support for virtual keyspace metadata queries (system_virtual_schema); allows the driver to expose metadata for virtual tables like `system.clients`.
- **Our fork:** Reproduces on otter-cache as of 4a1928a — feature not present (no `system_virtual_schema` references in our codebase).
- **Next step:** Track upstream.

---

### Recently merged (cherry-pick candidates)

**Cutoff:** 2026-05-14 (commit `8a77559` "Merge branch 'trunk' into otter-cache").

No PRs merged to upstream `trunk` between 2026-05-14 and 2026-05-17. Upstream merge activity is effectively nil; nothing to cherry-pick from this window. Re-run this section in the next sweep with the cutoff bumped to 2026-05-17.

## No-action bucket (S4/S5 or R4)

### Issues

- #1905 — Make gocqlx part of the package — S5/R4: gocqlx integration, out of scope
- #1887 — Need a load balancing policy that takes server node's latency into account — S4/R4: feature request; upstream PR #1831 exists (LatencyAwareHostPolicy); not our concern now
- #1877 — Add API to pass custom data to observers — S4/R4: observer API feature request
- #1872 — Waiting connection from pool — S4/R4: observability feature request, no bug
- #1869 — Observing Final Query Latency — S4/R4: observer feature request
- #1795 — Fix flaky tests — S4/R4: upstream CI maintenance, not our problem
- #1786 — CASSGO-39 Add query attempt interceptor — S4/R4: feature request; upstream PR #1943 in progress
- #1776 — Moving the repository to github.com/apache/cassandra-gocql-driver — S5/R4: infrastructure/migration discussion, resolved
- #1774 — Trying to get cassandra active/idle/inUse connection — S4/R4: metrics/observability feature request
- #1760 — Improve Error Handling — S4/R4: vague enhancement request, 0 comments
- #1742 — Using gocql for integration unit test but failed intermittently — S5/R4: user support/testing question
- #1735 — gocql: no response received from cassandra within timeout period — S5/R4: support question / user config issue
- #1733 — Database design — S5/R4: not a driver bug; user database design question
- #1731 — too many (100k) concurrent queries produce errors — S5/R4: user hitting stream/connection limits; expected behavior with 100k goroutines on one node
- #1720 — suggestion for Unmarshal version 2 — S5/R4: design/API suggestion, no bug
- #1710 — Commitlog handling — S5/R4: Cassandra-side question, not driver issue
- #1704 — Exec() returning ErrNotFound — S5/R4: DSE-specific / likely user bug; no reproduction with open-source Cassandra
- #1681 — Add an interface to refresh contact points — S4/R4: feature request
- #1679 — How to set pool size — S5/R4: support question
- #1674 — Default timeout in gocql.query — S5/R4: support/documentation question
- #1667 — New hosts connection details are not updated — S5/R4: old (2022), 2 comments, likely stale duplicate of ring-refresh issues
- #1657 — Always encode BigInt for time.Time — S4/R4: edge-case type marshaling behavior change request
- #1653 — Not able to build gocql in docker environment from v1.20 onwards — S5/R4: user build environment issue, not upstream driver bug
- #1651 — How to check if cassandra cluster session is connected (like PING for redis) — S5/R4: support question
- #1648 — QUERY ACCORDING TO DATE CONDITION — S5/R4: user support question
- #1644 — Does online doc is up to date? — S5/R4: docs question, no bug
- #1640 — Bad Behaviour in New v1.2.0 Release? — S5/R4: old (2022), likely stale version-change confusion, no actionable bug
- #1617 — Gigabytes of RAM usage with lz4/snappy enabled — S4/R4: compression codec memory issue; we use lz4/snappy but this is a known external lib issue; R4 since we track compressor separately
- #1603 — Authentication failed with Datastax Cassandra and LDAP+internal dual authentication — S5/R4: DSE/LDAP-specific, not Apache Cassandra
- #1601 — session.Closed returns false when cassandra went down — S4/R4: feature/API behavior gap; low priority
- #1596 — Is gocql/gocql appropriate for Scylla? — S5/R4: support question about Scylla compatibility
- #1589 — Merge ScyllaDB patches — S5/R4: Scylla-specific feature request
- #1586 — GoCql Iter doesn't return error in case of query read timeouts — S5/R4: labeled `question`; user misunderstanding — `iter.Close()` returns the error; not a bug
- #1578 — Question about decimal.Decimal support — S5/R4: support question about third-party decimal library
- #1577 — Override default database query timeouts — S5/R4: support question, documented API
- #1560 — Cannot achieve consistency level QUORUM — S5/R4: user misconfiguration (labeled `waiting-for-info`); not a driver bug
- #1516 — Schema changes don't reliably effect current session — S4/R4: KeyspaceMetadata cache invalidation enhancement; low priority ergonomics
- #1479 — Need a way to choose a specific host — S4/R4: feature request
- #1471 — Integrating gocql into OSS-fuzz — S4/R4: infrastructure enhancement; not our fork's concern
- #1459 — Export Metrics to Prometheus — S4/R4: observability feature request
- #1448 — Batch size in bytes — S4/R4: feature request for batch size limit enforcement
- #1331 — Scan of int value into `interface{}` fails — S4/R4: old (2019), 0 comments, likely stale/edge-case
- #1212 — Observers no longer running in calling goroutine — S4/R4: observer behavior change; intentional async design
- #1080 — Suboptimal struct alignment — S4/R4: micro-optimization, enhancement only
- #1072 — #1062 and #1068 causes perf degradation — S4/R4: old (2018) perf issue, likely stale
- ~~#953 — Session Timeout and Context Timeout~~ — **promoted out of no-action per Codex review**: PR #1866 (shortlist item 7) resolves this; #953 is the linked bug for that PR.
- #936 — after upgrade logon takes too long (ConnectTimeout) — S5/R4: labeled `waiting-for-info`; 2017 issue, likely stale user env issue
- #899 — Cassandra tick-tock releases — S5/R4: Cassandra version compatibility note, no driver action needed
- #845 — gocql prepares all queries — S5/R4: labeled `waiting-for-info`; user support question about prepared cache; use `NotPrepared()` API
- #818 — Add concept of host locality to query execution — S4/R4: feature request; DCAwareRoundRobin handles this now
- #806 — Does SliceMap support UDT unmarshalling? — S5/R4: support question
- #756 — Disconnect event notifications — S4/R4: observer/event API feature request
- #738 — ensure at least one initial host passes the host filter if present — S4/R4: enhancement; upstream PR #1867 (CASSGO-62) addresses this
- #1698 — Vault Cassandra Database Secrets Engine — S5/R4: Vault-specific integration question, out of scope for driver
- #688 — Integrate HostPool into the connection pool — S4/R4: old (2016) architectural feature request
- #647 — Simplify integration test runs — S4/R4: upstream CI/test infrastructure improvement
- #574 — Improve compatibility using the nodes version — S4/R4: old (2016) feature request for version-adaptive behavior
- #517 — marshal: add support for omitempty tags for UDTs — S4/R4: marshal feature request
- #465 — CASSGO-29 refactor packages — S4/R4: large internal API refactor; upstream PR #1857 in progress
- #418 — errors should indicate if they came from gocql directly — S4/R4: error wrapping ergonomics; partially addressed by our `6238950` %w commit
- #356 — Token ring awareness API — S4/R4: old (2015) API design discussion; superseded by existing TokenAwareHostPolicy

### PRs

- PR #1950 — [INFRA] Set up default rulesets for default and release branches — S5/R4: infrastructure/GitHub rulesets setup, no driver code change
- PR #1949 — CASSGO-123 Bump C* versions in CI (DRAFT) — S5/R4: CI/test infrastructure update, draft, no code change
- PR #1943 — CASSGO-39 Add exec attempt interceptor round 3 — S5/R4: feature request (interceptor API), not a bug fix; A3 if we want the feature eventually
- PR #1941 — Add Table field to ObservedQuery and ObservedBatch — S5/R4: observer API feature extension; additive but not a bug fix; track upstream
- PR #1921 — Adding total attempt metric for SpeculativeRetries — S5/R4: new metrics API; additive feature, not a correctness fix
- PR #1871 — Add QueryRequestObserver and BatchRequestObserver — S5/R4: new observer API types; additive feature request
- PR #1863 — CASSGO-55 User API doc enhancement — S4/R4: documentation improvement only; no code change
- PR #1857 — CASSGO-29 Refactor packages to separate internal and public API — S5/R4: large API refactor; upstream work in progress, out of scope for our fork now
- PR #1849 — CASSGO-46 Merge ring and ringDescriber — S5/R4: internal refactor to merge ring-related types; large structural change, out of scope
- PR #1831 — CASSGO-54 Add LatencyAwareHostPolicy — S5/R4: new host policy feature; useful eventually but not a correctness fix
- PR #1820 — CASSGO-39 Add query attempt interceptor — S5/R4: earlier round of the interceptor feature; superseded by #1943
- PR #1802 — CASSANDRA-19853 update issue templates and add PR template (DRAFT) — S5/R4: GitHub template/infra, draft, no code change
- PR #1784 — Adding support system_traces.coordinator_port and source_port — S5/R4: new column support in tracing tables; feature addition, not a bug fix
- PR #1777 — Correct name of function in godocs — S4/R4: godoc typo fix; tiny, can be cherry-picked trivially if desired but minimal value
- PR #1756 — Implemented token bucket rate limiter — S5/R4: new rate-limiting feature; out of scope for our fork
- PR #1753 — error messages refactor — S4/R4: error message wording refactor; non-trivial diff across many files, low priority
- PR #1747 — MaxRequestsPerConn cluster param was added — S5/R4: new configuration parameter feature; not a bug fix
- PR #1745 — Document that query timestamps are in Unix microseconds — S4/R4: godoc documentation improvement only
- PR #1737 — Rename centre -> center, fix typos — S4/R4: typo/spelling fix in comments only
- PR #1717 — Use strings.Builder instead of bytes.Buffer to build strings — S4/R4: micro-optimization refactor; low value
- PR #1715 — chore: use buf.String() instead of string(buf.Bytes()) — S4/R4: micro-style cleanup; negligible
- PR #1663 — Run vet and tests in all subpackages in CI — S5/R4: CI configuration improvement; no driver code change
- PR #1662 — Update test to pass with pierrec/lz4 4.1.14 — S4/R4: test compatibility fix for dependency version; low priority
- PR #1645 — Change default port if provided in new cluster — S3/R3: edge-case config behavior; stale (2022), last touched 2024-07-30; A3 track upstream
- PR #1637 — Use Cassandra 4.0 in integration tests — S5/R4: CI/test infrastructure update; no driver code change
- PR #1590 — Add version2 of function/aggregate metadata — S5/R4: new schema metadata feature; out of scope for our fork now
- PR #1565 — Add rack aware round-robin host policy — S5/R4: new host policy feature; out of scope for now
- PR #1558 — Fix flaky test TestStartupTimeout — S4/R4: test flakiness fix; upstream CI concern
- PR #1556 — Return table configuration parameters in TableMetadata — S5/R4: schema metadata feature addition; out of scope
- PR #1495 — [Code Optimization] huge parameters need to be passed as pointer — S4/R4: micro-optimization; stale (2020), last touched 2024-07-30
- PR #1489 — Add Duration parsing and string conversion functions — S5/R4: new utility functions for CQL Duration type; out of scope
- PR #1461 — remove unit tag, always run the unit tests — S5/R4: CI tag removal; upstream test infrastructure
- PR #1434 — Add a new error ErrKeyspaceCheckFailed for session init — S4/R3: new error type for keyspace check failure; additive API; A3 track
- PR #1307 — Partitioned batches — S5/R4: large new feature (batch partitioning); out of scope for our fork
- PR #1290 — make session immutable — S5/R4: large internal API refactor for immutable session; out of scope
- PR #1211 — Shard awareness extension for Scylla patchset 2 — S5/R4: ScyllaDB-specific shard-awareness feature; out of scope
- PR #1210 — Shard awareness extension for Scylla patchset 1 — S5/R4: ScyllaDB-specific shard-awareness feature; out of scope

---

## Codex consensus review (2026-05-17)

A second-opinion pass was performed by Codex against the original 14-item shortlist
(commit `4e1069c`). Codex agreed with 6 items outright, raised caveats on 5, and
disagreed on 3. Below: the verdicts and our disposition.

### Accepted corrections — already folded into the shortlist above

- **Item 3 (PR #1790):** Removed issue #1721 from the resolved list. #1721 is the
  changing-IP-on-reconnect family covered by PR #1722, not the peers-query
  suppression that #1790 addresses.
- **Item 6 (PR #1867):** Reworded from "silently produces an empty pool" to
  "fast-fail when filter rejects all hosts at init." Code actually returns
  `ErrNoConnectionsStarted` (`session.go:426-427`), late and lossy — #1867
  provides the early guard.
- **Item 8 (#1919):** Promoted from A2 to A1. Heartbeat at `conn.go:732` uses
  `context.Background()` then `conn.go:1441-1456` arms the connection timeout —
  patch direction is concrete: heartbeat needs a timeout independent of
  `Session.Timeout`.
- **Item 10 (#1259):** Demoted scope. TokenAware→HostPool fallback is covered
  by `policies.go:804-842`'s `used` map. Only standalone
  `hostpool.HostPoolHostPolicy.Pick` retains the unbounded behavior — lower
  priority. Bundled with #812 (item 11 → 9).
- **Item 12 (PR #1944):** Demoted from S2 to S3/A3. Internal lock-scope
  cleanup, not an S2 perf cliff without a benchmark.
- **Item 14 (PR #1722):** Removed "pick whichever is cleaner" language. #1722
  and #1790 fix different things; both may be needed.
- **#953:** Promoted out of the no-action bucket — it is resolved by PR #1866
  (shortlist item 7).
- **#1938:** Re-verified and **closed out (A0)**. `iter_mapscan_test.go:282`
  (`TestMapScan_UDT_PopulatedMapDistinctAcrossRows`) and `:338` from our
  `cfb8816` commit explicitly cover the top-level UDT MapScan path. The
  earlier Task 4 verdict was too pessimistic.
- **PR #1797 date:** Fixed typo `2026-08-14` → `2024-08-14`.

### Disagreed-with point we held

- **Item 5 (PR #1724 severity):** Codex flagged inconsistency between the
  issue row (#1723 at S3) and the PR row (#1724 at S2). We held at S2 for
  the PR because Codex's own evidence — `session.go:262-278` dials
  unfiltered hosts pre-handshake before `control.go:333-335` rejects them —
  shows the filter is bypassed on real network traffic, not just at a
  post-handshake check. A noted inconsistency, not a defect.

### Items Codex couldn't live-verify

- All `gh pr view` calls in Codex's run failed (no `api.github.com`
  reachability from its environment). Conclusions about cherry-pick
  cleanliness rely on our local artifact + repo state. When we actually
  apply the A5 PRs, fetch and read each diff against `otter-cache` HEAD.

### Items the consensus surfaced for the no-action bucket

- **PR #1645** is mis-bucketed (it's labeled `S3/R3/A3 track upstream`
  inside a bucket that should only hold S4/S5/R4). Moved to the
  Track-upstream tier of the shortlist preamble (and should be re-routed
  in the next sweep).
- **#1667** may be a duplicate of the ring-refresh/k8s A2 cluster. Left in
  the bucket for now — re-check in next sweep with an actual fetch of the
  issue body, since Codex could not verify it.

### Final shortlist counts (post-reconciliation)

- **A5 cherry-pick:** 7 distinct PRs (#1948, #1738, #1790, #1797, #1724,
  #1867, #1866). Plus PR #1866 also resolves #953.
- **A1 fix locally:** 2 items (#1919 with concrete patch direction, #1884
  pending reproducer).
- **A2 investigate:** 5 items (#1582, #812, #1259-standalone, PR #1729,
  PR #1722).
- **A3 track:** PR #1944, plus the prior A3 list.

### Disposition summary

Consensus reached. Codex's review caught one genuine miss (#1938 was already
fixed), one bucket leak (#953 belongs on the shortlist), one severity error
(PR #1944), one over-promised cross-reference (PR #1790 → #1721), and one
overstated description (PR #1867). All folded in.
